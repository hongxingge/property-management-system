<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.manage.mapper.ParkingChargeMapper">


    <update id="updateParkingCharge">
        UPDATE parking_charge
        SET parking_cost = #{parkingCharge}
            WHERE id = 1
    </update>

    <select id="getParkingCharge" resultType="float">
        SELECT parking_cost
        FROM parking_charge
        WHERE id = 1
    </select>

</mapper>
